// QUESTÃO NÃO FINALIZADA
const entrada = require("prompt-sync")()

function main(){

    const data_atual_dia = Number(entrada("Digite a data atual (dia): "))
    const data_atual_mes = Number(entrada("Digite a data atual (mês): "))
    const data_atual_ano = Number(entrada("Digite a data atual (ano): "))

    const data_nascimento_dia = Number(entrada("Digite a data do seu nascimento (dia): "))
    const data_nascimento_mes = Number(entrada("Digite a data do seu nascimento (mês): "))
    const data_nascimento_ano = Number(entrada("Digite a data do seu nascimento (ano): "))

    
}

main()